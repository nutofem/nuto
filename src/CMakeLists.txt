# $Id$
INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})

# add library
ADD_SUBDIRECTORY(nuto)
INSTALL(DIRECTORY nuto DESTINATION ${NUTO_INCLUDE_INSTALL_PATH} FILES_MATCHING PATTERN "*.h" PATTERN ".svn" EXCLUDE)

# add examples
# ADD_SUBDIRECTORY(examples)

# StrucutreGrid3D
ADD_EXECUTABLE(StructureGrid3D StructureGrid3D.cpp)
TARGET_LINK_LIBRARIES(StructureGrid3D NuToMechanics NuToMath NuToBase NuToOptimize ${Boost_LIBRARIES} ${ANN_LIBRARIES})
IF (ENABLE_VISUALIZE)
  TARGET_LINK_LIBRARIES(StructureGrid3D NuToVisualize)
ENDIF (ENABLE_VISUALIZE)
IF(MUMPS_FOUND)
  TARGET_LINK_LIBRARIES(StructureGrid3D ${MUMPS_LIBRARIES} ${CMAKE_THREAD_LIBS_INIT})
ENDIF(MUMPS_FOUND)
IF(MINGW)
  SET_TARGET_PROPERTIES(StructureGrid3D PROPERTIES LINK_FLAGS -Wl,--enable-auto-import)
ENDIF(MINGW)
