#test files for math module
IF(ENABLE_PYTHON_TESTS)
  SET( CMAKE_VERBOSE_MAKEFILE on )

  ADD_TEST(optimize::ConjugateGradientNonLinear ${PYTHON_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/ConjugateGradientNonLinear.py ${CMAKE_SYSTEM_NAME} ${CMAKE_SYSTEM_PROCESSOR} ${CMAKE_CURRENT_SOURCE_DIR})
  SET_PROPERTY(TEST optimize::ConjugateGradientNonLinear PROPERTY ENVIRONMENT "PYTHONPATH=${CMAKE_BINARY_DIR}/src")

ENDIF(ENABLE_PYTHON_TESTS)

