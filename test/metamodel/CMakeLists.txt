#test files for math module
IF(ENABLE_PYTHON_TESTS)
  SET( CMAKE_VERBOSE_MAKEFILE on )
  ADD_TEST(metamodel::NeuralNetwork ${PYTHON_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/NeuralNetwork.py ${CMAKE_SYSTEM_NAME} ${CMAKE_SYSTEM_PROCESSOR} ${CMAKE_CURRENT_SOURCE_DIR})
ENDIF(ENABLE_PYTHON_TESTS)

